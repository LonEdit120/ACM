#include <iostream>
#include <string>

using namesapce std;

int main()
{
	string s, stop, start, end;
	s = "string";
	stop = "ENDOFINPUT";
	start = "START";
	end = "END";
	int input[200];
	int count = 0;
	int x = 'A'-1;
	while(s != stop)
	{
		int x = 'A'-1;
		char A;
		getline(cin, s);
		if(s = start && s != stop)
		{
			while(s != end && s != stop)
			{
				count++;
				
			}
		} 
	}
}